# File: homework1. py 
# --- Variables and Data Types --- 
a = 10
print(a)
print(type(a))
# a this is an integer, a whole number with no decimals
b = 1.5
print (b)
print(type(b)) 
#this is a float, a fractional number
c = 3j 
print(c)
print(type(c))
#this is an complex object, composed of a real(number) and imaginary part(j)
d = "hello"
print(d)
print(type(d))
#this is a string, used to store a sequence of characters
e = [1,2,3]
print(e)
print(type(e))
#this is a list, used to store mulitple items in a single variable. They are ordered, but unlike tuples, they are changeable. 
f = {"name": "Ellen", "favorite fruit": "strawberry"}
print(f)
print(type(f))
#this is a dictionary, used to store data values in key: value pairs
g = (1,2)
print(g)
print(type(g))
#this is a tuple, used to store multiple items in a single variable. It is unchangeable and ordered
h = ["apple", "banana", "strawberry"]
print(h)
print(type(h))
#this is a list
i = True 
print(i)
print(type(i))
#this is a boolean, a True or False value 
j = None
print(j)
print(type(j))
#this is NoneType, a data type that represents the abscence of a value
k = [True, "blue", 12] 
print(k)
print(type(k))
#this is list
l = str(14)
print(l)
print(type(l))
#this is a str() function, which converts the specified valiue into a string
m = 1e4
print(m)
print(type(m))
# #this is a float
# # #QUESTIONS: 
# # 1. I found 10 different data types
# # 2. Integer, String, Float, Boolean, Complex, List, Dictionary, Tuple, NoneType, Str()
# # 3. integer (a,), String (d, (also a str()= l), Floats (b, m), Complex (c), Lists( e, h, k), Dictionary (f), Tuple (g), Boolean (i), Nonetype (j)
# 4. l was a string, str() converts the given input into a sting.
# 5. I tried a frozenset, which is a set that can not be modified. It also cannot have duplicate values
n = frozenset(["hi", "hello", "bye"])
print(n)
print(type(n))
print("hi" in n)
print("what" in n)
#the prints return a true or false value 
# --- Variables and Data Types ---
print(10>9) #True, 10 is greater than 9
print (10==9) #Fales, 10 is not equal to 9
print (10 <= 9) #False, 10 is not greater than or equal to 9
print(bool("abc")) #True, there is a value within the string 
print(bool(123)) #, there is an integer present
print(bool(["apple", "cherry", "banana"])) #True, there are string values
print(bool(True)) #true, since the value os True
print(bool(False)) #False, since the value is False
print(bool(0)) #False, because 0 is considered a false value
print(bool("")) #False, because it is an empty string
print(bool(" ")) #True, because although it may look empty, it has a space in it
print(bool()) #False, because there is nothing within the parentheses
print(bool([])) #False, because there is nothing within the objects
print(bool({})) #False, because there is nothing within the brackets, nothing is being described so it can only return False 
print(bool(True and False)) #False, because True is not False 
print(bool(True and True)) #True because both are True
print(bool(False and False)) #False because both are False
print(bool(True or False)) # True, there is at least one True value, so it returns True
print(bool(True or True)) #True, both are True values, so it returns True
print(bool(False or False)) #False, because both are False values so it returns False
print(bool(not(False))) #True, opposite of False
print(bool(not(True))) #False, opposite of True
# QUESTIONS: 
# 1. I noticed that they would tend to be True or False depending on the type of data within it
# Such as with the fruits and numbers, they were all the same data, 
# strings and integers respectively.
# 2. The False or False one, since I thought it would be marked as true as they're both False. 
# 3 , this printed True since they are all animals
print(bool(["dog", "apple", "lion"]))
#4 : It is false because j does not equal 1.
print(bool(j==1))
# --- Variables and Data Types ---
print(10+5) #15, performs addition
print(10-5) #5, performs subtractions
print(2*4) #8, perfroms mulitiplication
print(6/3) #2.0, performs divison
print(5%2) #1, remainders
print(3**2) #9, performs exponentials
print(15//2) #7, performs Floor Division, which divides and rounds the result to the neareast whole number
print(5==2)#False, evaluates whether a equals b
print(10!=10) #False, this check whether the two values are different "not equal to"
print(2>5)#False, check whether a is greater than b
print(5<=6)#True, this checks whether a is less than OR equal to b
print(1>=10)#False, checks whether a is greater than OR less than to b
x=5 
x +=5
print(x)
x -=4
print(x)
x *=3
print(x) 
# logical operator questions
# 1. The and operator is used to combine two or more conditional statements
# , it is to test if conditions are met.
# or it also tests for true of false values, if a and b both true, returns b. 
# If a or b not true, returns the false value
y=False
z=True
w=True
u=False
print(z and w)
print(y and z)
# 2. or evaluates multiple conditions and returns true if at least one of the conditions are True, returns false
# if all the conditions are False
print(z or u)
print(y or u)
# 3. The operator not inverts the truth value of the variable
print (not y)
print(not w)
# QUESTIONS
# 1. The difference is that / performs division which includes the decimal
# whereas // only returns a whole number close to the output
# 2. % returns remainders  and // is more of division
# 3. I'd use the % operator
print (10%4) #since 4 goes into 10 twice, with 2 remaining, it returns 2.
# 4. Assignment operators work with changing the value of the variable according to the operator and the 
# value chosen. a += b adds b to a, a -=b subtracts b from a , and a*=b multiplies a with b.
# --- Variables and Data Types
my_string="hello"
print(my_string) #prints hello
print(my_string[0])#prints the first character in "hello", h
print(my_string[1])#prints the 2nd character in "hello", e
print(my_string[2])#prints the 3rd character in "hello", l
print(my_string[3])#prints the 4th character in "hello", l
print(my_string[4])#prints the 5th character in "hello", o
print(my_string[-1])#prints o, since it went back a character starting from 0, so it woukd go to o, the last character in the string
print(my_string[1:3])# prints the 2nd and 4th characters in hello
print(my_string[0:5:2])#prints hlo. it starts at 0, the first character, prints h, goes forward 5, where it stays at h
#then it prints every other character (hence 2), not including the h from 5. 
print(len(my_string))#prints the number of character in "hello", being 5
print(my_string+"goodbye")#prints "hellogoodbye", added both of the words together
print(my_string*7)#prints "hello" 7 times 
#QUESTIONS
# 1. Slicing is when you choose characters where you specify a start, stop and step. I slcied the 8th and 9th mutations
#2
name="Oski" #creates the string variable name , with oski
print("Hello, my name is {name}") #prints Hello, my name is {name}
#3
name="Oski" #creates name variable
print(f"Hello, my name is {name}") #prints, "Hello, my name is Oski"
#The difference lies with the f. In the 3rd questions, it prints {name} instead of Oski, this is because it was 
#seen as an ordinary string. But the f-string provides a way to insert a variable into a string, so it replaces {name} with the
#value of the variable.
# --- Variables and Data Types
#cd 
#changes directories, you can use it to move from one folder to another
#ex, cd homework1
#ls
#ls lists out the contents of a folder, and you see what is inside the directory
#ex ls python_decal_sp26
#ls -a also lists out all files in the current directory, but the -a stands for 'all" so it includes hidden files
#ls -a python_decal_sp26
#mkdir 
#mkdir stands for "make directory", so it creates a directory
#ex mkdir test1
#cat
#cat stands for "concatenate" and it is usually used to show the contents of a file
#ex. cat mathfun.py
#pwd 
#pwd prints the current working directory, so the directory I am in, showing the full path of the folder I'm in
#ex pwd
#cd ..
#cd .. changes directory, where ".." stands for the parent folder of my current folder
#ex cd ..
#cd . 
#cd . changes the directory, but to the current directory you are in. So, nothing really changes
#ex cd . 
#cd ~ 
#cd ~ since "~" stands for the home folder, this changes the directory to the home folder
#ex cd ~
#cp mathfun,py
#cp "copy", this command copies files or folders
#mv
#mv stands for "move" (or rename), it's a command that either moves files/folders or renames them
#ex mv test1/ python_decal_sp26
#rm
#rm means "remove", it deletes files and folders. This cannot be undone
#rm funnylizardtxt
#clear
#clear simply cleans the terminal screen, like a new page, so it doesn't delete anything.
#ex clear
#grep
#grep is a search tool, it is very versatile and can search for words in files/multiple, folders, show line numbers, or inverse searches.
#grep 2 mathfun.py
#QUESTIONS
# history- this command allows me to view my recent commands. touch- touch allows me to create an empty file; I can also move it where I want 
#with this command as well. curl- curl allows me to basically access the internet and use data. I can download with this as well.
#2.ls -a prints out all the content in the directory, including any hidden files. Whereas ls does not include anything that is hidden.
#3. A hidden file is usually a configuration file, system files, or settings for projects. They are simply hidden because they do not need to be altered.
#4. ls -t : this command lists out all files in the directory by modification time. rm -r deletes a folder/directory and all its files/subfolders. 
#rm -f is basically a force delete, it deletes without asking and ignores errors. 